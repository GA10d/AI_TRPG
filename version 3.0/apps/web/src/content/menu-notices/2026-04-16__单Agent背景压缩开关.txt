今天的公告更新如下：

1. Beginning 界面最左侧设置栏在选择单 Agent 叙事时，新增了“背景压缩”开关。
2. 开启后，Narrator 会继续使用压缩后的 context pack；关闭后，Narrator 会直接读取完整公共上下文。
3. 这个开关只影响单 Agent 主持人的叙事上下文，不会改动多 Agent 路径，也不会改动辅助 AI 的处理方式。
4. 辅助 AI 仍然只根据当前主持人这一轮的返回来判断是否进入结局，方便你单独对比压缩版和全量上下文版的叙事效果。
