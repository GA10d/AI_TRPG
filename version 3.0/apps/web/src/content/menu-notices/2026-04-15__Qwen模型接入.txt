今天的公告更新如下：

1. 项目已经接入 Qwen 文本模型，设置页里可以直接选择 `Qwen` 作为文字模型档案。
2. 同时接入了 DashScope 文生图链路，当前可选图片模型包括 `Wan 2.7 Image Pro`、`Qwen Image 2.0 Pro` 和 `Z-Image Turbo`。
3. 前端模型选择、服务端转发和漫画生成流程都已经串通，选择对应模型后可以直接用于开场、主持人回复、普通文生图和世界线漫画生成。
4. 当前默认 Qwen endpoint 已调整到国际站新加坡区；如果你的 key 也是新加坡工作区创建的，填入 `QWEN_API_KEY` 后即可直接使用。
