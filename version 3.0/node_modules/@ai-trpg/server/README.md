# Server App

`apps/server` 负责 3.0 的会话状态、内容加载、模型调用编排和调试接口。

当前已包含：

- 最小内容加载器
- 内容验证脚本
- Session 创建接口
- mock turn 接口
- Model Gateway 基线
- 供前后端复用的共享类型接入点
- 如果存在 `apps/web/dist`，可直接托管前端构建产物

后续阶段将在这里继续补：

- 更正式的 Session Service
- server_proxy 真正接通
- Save / Load
- Replay 导出
